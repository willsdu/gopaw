package routers

// 这个包对应 copaw/src/copaw/app/routers，
// 主要用于定义 HTTP 路由分组（如 workspace、providers、console 等）。
//
// 在 Go 版本中，你可以在这里定义：
//   - 路由注册函数（例如 RegisterRoutes(mux *http.ServeMux)）
//   - 各个子模块的 handler 函数
//
// 目前只是结构占位，方便后续逐步迁移具体逻辑。

